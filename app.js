/**
 * Entry point — logic lives in js/ modules (see js/ARCHITECTURE.md).
 * Bump ?v= on this import after js/ changes so the browser reloads the module graph.
 */
import { boot } from "./js/runtime.js?v=20260528mobileclip";

boot();
